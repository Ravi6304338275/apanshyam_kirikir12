# name: Daily LinkedIn Job Scraper

# on:
#   # Schedule: Run at 10:30 PM UTC every day
#   # Note: Adjust timezone as needed (UTC is default)
#   schedule:
#     - cron: '30 22 * * *'  # 10:30 PM UTC
  
#   # Allow manual trigger for testing
#   workflow_dispatch:
#     inputs:
#       location:
#         description: 'Job location to search'
#         required: false
#         default: 'United States'
#         type: string

# jobs:
#   scrape:
#     runs-on: ubuntu-22.04
    
#     # Prevent concurrent runs
#     concurrency:
#       group: linkedin-scraper-${{ github.ref }}
#       cancel-in-progress: false
    
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
      
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#           cache: 'pip'
      
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
      
#       - name: Install Chrome and Xvfb
#         run: |
#           # Install Chrome
#           wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
#           echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
#           sudo apt-get update
#           sudo apt-get install -y google-chrome-stable xvfb
          
#           # Verify installation
#           google-chrome --version
#           which google-chrome
      
#       - name: Run LinkedIn Scraper
#         env:
#           SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
#           SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
#           LOCATION: ${{ github.event.inputs.location || 'United States' }}
#           MAX_WORKERS: '8'
#         run: |
#           # Start Xvfb for virtual display
#           Xvfb :99 -screen 0 1920x1080x24 &
#           export DISPLAY=:99
          
#           # Run the scraper
#           python run.py
      
#       - name: Upload Results CSV
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: linkedin-jobs-csv
#           path: linkedin_jobs_*.csv
#           retention-days: 30
#           if-no-files-found: warn
      
#       - name: Upload Logs
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: logs
#           path: logs/
#           retention-days: 7
#           if-no-files-found: warn
      
#       - name: Send Notification on Success
#         if: success()
#         run: |
#           echo "✅ LinkedIn scraper completed successfully at $(date)"
#           echo "Check the Actions tab for results"
      
#       - name: Send Notification on Failure
#         if: failure()
#         run: |
#           echo "❌ LinkedIn scraper failed at $(date)"
#           echo "Check the Actions tab for logs"





























# name: LinkedIn Job Scraper

# on:
#   schedule:
#     - cron: '0 */6 * * *'
#   workflow_dispatch:

# jobs:
#   scrape:
#     runs-on: ubuntu-22.04

#     concurrency:
#       group: linkedin-scraper
#       cancel-in-progress: false

#     steps:

#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#           cache: 'pip'

#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt

#       - name: Run scraper
#         env:
#           SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
#           SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
#           LOCATION: United States
#           MAX_WORKERS: 8
#         run: |
#           python run.py























# name: LinkedIn Job Scraper

# on:
#   schedule:
#     - cron: '0 */6 * * *'
#   workflow_dispatch:

# jobs:
#   scrape:
#     runs-on: ubuntu-22.04

#     concurrency:
#       group: linkedin-scraper
#       cancel-in-progress: false

#     steps:

#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#           cache: 'pip'

#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt

#       - name: Run scraper
#         env:
#           SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
#           SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
#           LOCATION: United States
#           MAX_WORKERS: 8
#         run: |
#           python run.py
























# name: LinkedIn Job Scraper

# on:
#   schedule:
#     - cron: '0 */6 * * *'
#   workflow_dispatch:

# jobs:

#   scrape:

#     runs-on: ubuntu-22.04

#     concurrency:
#       group: linkedin-scraper
#       cancel-in-progress: false

#     steps:

#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#           cache: pip

#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt

#       - name: Run scraper
#         env:
#           SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
#           SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
#           LOCATION: United States
#           MAX_WORKERS: 8
#         run: |
#           python run.py























# name: LinkedIn Job Scraper

# on:
#   schedule:
#     # Runs at:
#     # 03:00 PM IST
#     # 09:00 PM IST
#     # 03:00 AM IST
#     # 09:00 AM IST
#     - cron: '30 3,9,15,21 * * *'
#   workflow_dispatch:

# jobs:

#   scrape:

#     runs-on: ubuntu-22.04

#     concurrency:
#       group: linkedin-scraper
#       cancel-in-progress: false

#     steps:

#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#           cache: pip

#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt

#       - name: Run scraper
#         env:
#           SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
#           SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
#           LOCATION: United States
#           MAX_WORKERS: 8
#         run: |
#           python run.py
